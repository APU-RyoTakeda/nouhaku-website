/** @type {import('next').NextConfig} */
const nextConfig = {
  // ここに既存のNext.js設定があればコピーしてください
  // 例: images: { domains: ['example.com'] },
  // 例: experimental: { appDir: true },
};

export default nextConfig;